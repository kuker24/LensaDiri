name: CI

on:
  push:
    branches: [main, "agent/**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  AUTH_SESSION_SECRET: ci-auth-session-secret-at-least-32-characters
  CSRF_SECRET: ci-csrf-secret-at-least-32-characters
  DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
  NEXT_PUBLIC_APP_URL: http://127.0.0.1:3000
  RATE_LIMIT_SECRET: ci-rate-limit-secret-at-least-32-characters
  TOKEN_HASH_PEPPER: ci-token-hash-pepper-at-least-32-characters

jobs:
  quality:
    name: Quality and build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          cache: npm
          node-version: 22

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Format
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Capture typecheck report
        run: |
          npx next typegen > typecheck-report.txt 2>&1 || true
          npx tsc --noEmit --pretty false >> typecheck-report.txt 2>&1 || true

      - name: Upload typecheck report
        uses: actions/upload-artifact@v4
        with:
          name: typecheck-report-${{ github.sha }}
          path: typecheck-report.txt
          retention-days: 1

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Production build
        run: npm run build

      - name: Dependency audit
        run: npm audit

      - name: Diff whitespace check
        run: git diff --check origin/main...HEAD

  database-and-e2e:
    name: Database and browser tests
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          cache: npm
          node-version: 22

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Start disposable Supabase
        run: npm run db:start

      - name: Three clean-reset verification loops
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          for iteration in 1 2 3; do
            echo "Clean reset loop ${iteration}/3"
            npm run db:reset
            npm run test:seed-replay
            npm run test:seed-replay-drift
            npm run test:integration
            npm run test:db
          done

      - name: Reset disposable database for browser fixtures
        run: |
          npm run db:reset
          docker exec supabase_db_lensa-diri psql -U postgres -d postgres -v ON_ERROR_STOP=1 \
            -c "update public.feature_flags set enabled = true where key in ('FEATURE_MODULAR_COMPOSER', 'FEATURE_COMPLEX_MODE')"

      - name: Run Playwright desktop and mobile
        run: npm run test:e2e

      - name: Run accessibility routes
        run: npm run test:a11y

      - name: Stop disposable Supabase
        if: always()
        run: npx supabase stop --no-backup
